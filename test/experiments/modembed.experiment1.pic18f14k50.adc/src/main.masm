module main target microchip.pic18{

	instance adcdriver adc;

	instance microchip.pic18.PIC18F14K50 cpu;

	/*
	 * Main function
	 */
	func main{
		call 0 adc.init;
		label refresh;
		call 0 adc.read;
		goto refresh;
		return 0;
	};
}
